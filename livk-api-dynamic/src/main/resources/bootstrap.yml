server:
  port: 5599
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  application:
    name: @artifactId@
  redis:
    host: ${REDIS_HOST:livk.com}
    password: ${REDIS_PASSWORD:123456}
    port: ${REDIS_PORT:6379}
  kafka:
    bootstrap-servers: ${KAFKA_HOST:livk.com}:${KAFKA_PORT:9092}
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:livk.com}:${NACOS_PORT:8848}
  boot:
    admin:
      client:
        url: lb://livk-api-monitor
        username: livk
        password: 123456
        instance:
          prefer-ip: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:livk.com}:${MYSQL_PORT:3306}/${MYSQL_DB:sc_gateway}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:123456}
    hikari:
      schema: classpath:sql/table.sql
  profiles:
    active: @profiles.active@
mybatis-plus:
  global-config:
    banner: false
swagger:
  enable: true
  version: @version@
management:
  endpoints:
    web:
      exposure:
        include: '*'
